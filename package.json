{
  "name": "bizing",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel --if-present --filter=!@bizing/canvascii-collab --filter=!@bizing/canvascii dev",
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "db:generate": "bun run --cwd packages/db db:generate",
    "db:migrate": "bun run --cwd packages/db db:migrate",
    "db:seed": "bun run --cwd packages/db db:seed",
    "db:guard": "bun run --cwd packages/db db:guard",
    "docs:check": "node scripts/docs-sync-check.mjs && node scripts/generate-domain-docs.mjs --check && node scripts/check-atlas-sync.mjs",
    "docs:generate:domains": "node scripts/generate-domain-docs.mjs",
    "docs:check:domains": "node scripts/generate-domain-docs.mjs --check",
    "docs:check:atlas-sync": "node scripts/check-atlas-sync.mjs",
    "docs:sync:mind": "node scripts/sync-body-docs-to-mind.mjs",
    "agent:fitness": "bash mind/workspace/run-agent-fitness-loop.sh",
    "agent:code-mode:mcp": "bash scripts/run-code-mode-mcp.sh"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "dependencies": {
    "ulid": "^3.0.2"
  }
}
