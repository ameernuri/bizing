{
  "name": "@bizing/db",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "db:generate": "drizzle-kit generate:pg",
    "db:migrate": "bun scripts/migrate.ts",
    "db:push": "drizzle-kit push:pg",
    "db:seed": "bun scripts/seed.ts",
    "db:studio": "drizzle-kit studio",
    "db:guard": "bun scripts/schema-guard.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.0",
    "ksuid": "^3.0.0",
    "pg": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "@types/pg": "^8.0.0",
    "tsx": "^4.0.0",
    "drizzle-kit": "^0.20.0",
    "dotenv": "^17.2.4"
  }
}
